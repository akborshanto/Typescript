"use strict";
{
    //spred operator
    //rest operation
    //destructure
    //spored operationr
    const boros1 = ['akbor', 'shanto'];
    const borso2 = ['bolla', 'nahid', 'rahat'];
    boros1.push(...borso2);
    const metors = {
        typescipt: 'mezzba',
        redux: "mir", dbms: "Mizan"
    };
    const metors2 = {
        typescipt: 'kashem',
        redux: "josim", dbms: "roudi"
    };
    const mentorLis = Object.assign(Object.assign({}, metors), metors2);
    //learn restoperatior
    // const greetFriends = (...friends: string[]) => {
    //    // console.log(`${friends[0]} ${friends[1]}`);
    //    friends.forEach((frind:string)=>console.log(`${fr1} ${fr2}`))
    //   }
    //   greetFriends('abul', 'tabul');
}
